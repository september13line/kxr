# Enhanced AI - Micro

####################################################################################################
# STRATEGIC POSTURES
####################################################################################################

ai_we_winning_big_time = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		alliance_strength_ratio > 1.25
		has_green_air = yes
		has_manpower_and_equipment_surplus = yes
		has_manpower_or_equipment_crisis = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 1.25 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.25 }
			strength_ratio = { tag = FROM ratio < 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush_weak
	}
}

ai_we_winning = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		alliance_strength_ratio > 1.25
		OR = {
			has_green_air = no
			has_manpower_and_equipment_surplus = no
		}
		has_manpower_or_equipment_shortage = no
		has_manpower_or_equipment_crisis = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 1.00 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.00 }
			strength_ratio = { tag = FROM ratio < 1.25 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.25 }
			strength_ratio = { tag = FROM ratio < 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush_weak
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = balanced
	}
}

ai_we_kinda_winning = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		alliance_strength_ratio > 1.25
		OR = {
			has_green_air = no
			has_manpower_and_equipment_surplus = no
		}
		has_manpower_or_equipment_shortage = yes
		has_manpower_or_equipment_crisis = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 0.75 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.75 }
			strength_ratio = { tag = FROM ratio < 1.00 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.00 }
			strength_ratio = { tag = FROM ratio < 1.25 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush_weak
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.25 }
			strength_ratio = { tag = FROM ratio < 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = balanced
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
	}
}

ai_we_stalemating = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		alliance_strength_ratio < 1.25
		alliance_strength_ratio > 0.75
		has_manpower_or_equipment_crisis = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 0.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.50 }
			strength_ratio = { tag = FROM ratio < 0.75 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.75 }
			strength_ratio = { tag = FROM ratio < 1.00 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush_weak
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.00 }
			strength_ratio = { tag = FROM ratio < 1.25 }
		}
		priority = 1000
		ordertype = front
		execution_type = balanced
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.25 }
			strength_ratio = { tag = FROM ratio < 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
		execute_order = no
		manual_attack = no
	}
}

ai_we_kinda_losing = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		alliance_strength_ratio < 0.75
		OR = {
			has_red_air = no
			has_manpower_or_equipment_shortage = no
		}
		has_manpower_and_equipment_surplus = yes
		has_manpower_or_equipment_crisis = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 0.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.50 }
			strength_ratio = { tag = FROM ratio < 0.75 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush_weak
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.75 }
			strength_ratio = { tag = FROM ratio < 1.00 }
		}
		priority = 1000
		ordertype = front
		execution_type = balanced
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.00 }
			strength_ratio = { tag = FROM ratio < 1.25 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.25 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
		execute_order = no
		manual_attack = no
	}
}

ai_we_losing = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		alliance_strength_ratio < 0.75
		OR = {
			has_red_air = no
			has_manpower_or_equipment_shortage = no
		}
		has_manpower_and_equipment_surplus = no
		has_manpower_or_equipment_crisis = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 0.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush_weak
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.50 }
			strength_ratio = { tag = FROM ratio < 0.75 }
		}
		priority = 1000
		ordertype = front
		execution_type = balanced
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.75 }
			strength_ratio = { tag = FROM ratio < 1.00 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.00 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
		execute_order = no
		manual_attack = no
	}
}

ai_we_losing_big_time = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		alliance_strength_ratio < 0.75
		has_red_air = yes
		has_manpower_or_equipment_shortage = yes
		has_manpower_or_equipment_crisis = no
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 0.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = balanced
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.50 }
			strength_ratio = { tag = FROM ratio < 0.75 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 0.75 }
		}
		priority = 1000
		ordertype = front
		execution_type = careful
		execute_order = no
		manual_attack = no
	}
}

####################################################################################################
# OVERRIDES
####################################################################################################

ai_we_civil_warring = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio < 1.00 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
		execute_order = yes
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.00 }
			strength_ratio = { tag = FROM ratio < 1.25 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.25 }
			strength_ratio = { tag = FROM ratio < 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = rush_weak
	}

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
			strength_ratio = { tag = FROM ratio > 1.50 }
		}
		priority = 1000
		ordertype = front
		execution_type = balanced
	}
}

ai_we_replenishing = {
	enable = {
		has_war = yes
		has_an_actual_kx_civil_war = no
		has_manpower_or_equipment_crisis = yes
	}
	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		country_trigger = {
			has_war_with = FROM
		}
		priority = 1000
		ordertype = front
		execution_type = careful
		execute_order = no
		manual_attack = no
	}
}

####################################################################################################
# CORE DEFENSE
####################################################################################################

protect_homeland_1 = {
	enable = {
		surrender_progress > 0.05
	}
	abort = {
		OR = {
			surrender_progress < 0.02
			has_capitulated = yes
		}
	}
	
	ai_strategy = {
		type = front_unit_request
		state_trigger = { 
			any_state = {
				is_core_of = FROM.FROM
				controller = {
					has_war_with = FROM.FROM
				}
			}
		}
		value = 50
	}
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			NOT = { is_core_of = FROM.FROM }
		}
		value = -50
	}
	ai_strategy = {
		type = dont_defend_ally_borders
		value = 50
	}
}

protect_homeland_2 = {
	enable = {
		surrender_progress > 0.10
	}
	abort = {
		OR = {
			surrender_progress < 0.05
			has_capitulated = yes
		}
	}
	
	ai_strategy = {
		type = front_unit_request
		state_trigger = { 
			any_state = {
				is_core_of = FROM.FROM
				controller = {
					has_war_with = FROM.FROM
				}
			}
		}
		value = 100
	}
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			NOT = { is_core_of = FROM.FROM }
		}
		value = 100
	}
	ai_strategy = {
		type = dont_defend_ally_borders
		value = 100
	}
}

protect_homeland_3 = {
	enable = {
		surrender_progress > 0.20
	}
	abort = {
		OR = {
			surrender_progress < 0.10
			has_capitulated = yes
		}
	}
	
	ai_strategy = {
		type = front_unit_request
		state_trigger = { 
			any_state = {
				is_core_of = FROM.FROM
				controller = {
					has_war_with = FROM.FROM
				}
			}
		}
		value = 500
	}
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			NOT = { is_core_of = FROM.FROM }
		}
		value = -500
	}
	ai_strategy = {
		type = dont_defend_ally_borders
		value = 500
	}
}

protect_homeland_4 = {
	enable = {
		surrender_progress > 0.40
	}
	abort = {
		OR = {
			surrender_progress < 0.20
			has_capitulated = yes
		}
	}
	
	ai_strategy = {
		type = front_unit_request
		state_trigger = { 
			any_state = {
				is_core_of = FROM.FROM
				controller = {
					has_war_with = FROM.FROM
				}
			}
		}
		value = 1000
	}
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			NOT = { is_core_of = FROM.FROM }
		}
		value = -1000
	}
	ai_strategy = {
		type = dont_defend_ally_borders
		value = 1000
	}
}

####################################################################################################
# FRONTLINE AI UTILITIES
####################################################################################################

end_pocket = {
	enable = {
		has_war = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		state_trigger = {
			OR = {
				AND = {
					is_controlled_by = FROM.FROM
					NOT = { is_fully_controlled_by = FROM.FROM }
				}
				controller = {
					has_war_with = FROM.FROM
				}
			}
			all_neighbor_state = {
				controller = {
					NOT = { has_war_with = FROM.FROM }
				}
			}
		}
		ratio = 0.0
		priority = 5000
		ordertype = front
		execution_type = rush
		execute_order = yes
		manual_attack = yes
	}

	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			OR = {
				AND = {
					is_controlled_by = FROM.FROM
					NOT = { is_fully_controlled_by = FROM.FROM }
				}
				controller = {
					has_war_with = FROM.FROM
				}
			}
			all_neighbor_state = {
				controller = {
					NOT = { has_war_with = FROM.FROM }
				}
			}
		}
		value = 40
	}
}

forced_check_omega_lul = {
	enable = {
		has_war = yes
	}

	abort_when_not_enabled = yes

	# Deprioritize states without supply nodes
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			is_controlled_by = FROM.FROM
			NOT = {
				any_province_building_level = {
					province = {
						all_provinces = yes
					}
					building = supply_node
					level > 0
				}
			}
		}
		value = -5
	}

	# Prioritize capital area
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			OR = {
				is_capital = yes
				any_neighbor_state = { is_capital = yes }
			}
		}
		value = 15
	}

	# Heavily prioritize own capital
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			is_capital = yes
			owner = { tag = FROM.FROM }
		}
		value = 50
	}

	# Reinforce supply hub corridors
	ai_strategy = {
		type = front_unit_request
		state_trigger = {
			is_controlled_by = FROM.FROM
			any_neighbor_state = {
				any_province_building_level = {
					province = { all_provinces = yes }
					building = supply_node
					level > 0
				}
			}
		}
		value = 10
	}

	# Deprioritize low-value naval invasions
	ai_strategy = {
		type = invasion_unit_request
		state_trigger = {
			NOT = { has_state_flag = recent_naval_invasion_flag }
			check_variable = { state_strategic_value < 3 }
		}
		value = -5
	}

	ai_strategy = {
		type = invasion_unit_request
		state_trigger = {
			NOT = { is_core_of = FROM.FROM }
			NOT = { has_state_flag = recent_naval_invasion_flag }
			check_variable = { state_strategic_value < 8 }
		}
		value = -3
	}

	ai_strategy = {
		type = invasion_unit_request
		state_trigger = {
			NOT = { is_core_of = FROM.FROM }
			NOT = { has_state_flag = recent_naval_invasion_flag }
		}
		value = -3
	}

	ai_strategy = {
		type = invasion_unit_request
		state_trigger = {
			NOT = { is_core_of = FROM.FROM }
			NOT = { has_state_flag = recent_naval_invasion_flag }
			NOT = {
				any_province_building_level = {
					province = {
						all_provinces = yes
					}
					building = supply_node
					level > 0
				}
			}
		}
		value = -2
	}
}

this_state_is_empty = {
	enable = {
		has_war = yes
	}

	abort_when_not_enabled = yes

	ai_strategy = {
		type = front_control
		state_trigger = {
			is_controlled_by = FROM
			any_neighbor_state = {
				is_controlled_by = FROM.FROM
			}
			controller = {
				divisions_in_state = {
					state = PREV
					size < 1
				}
			}
		}
		ratio = 0.0
		priority = 10000
		ordertype = front
		execution_type = rush
		execute_order = yes
		manual_attack = yes
	}
}
